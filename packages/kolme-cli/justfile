set fallback := true

# List all recipes
default:
    just --list --unsorted

# Show public key
public:
    cargo run -- pub-key

# Fork height information
fork-height:
    cargo run -- fork-height

# Check version
check-version:
	cargo run -- check-version 0.1
