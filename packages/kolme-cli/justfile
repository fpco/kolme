set fallback := true

# List all recipes
default:
    just --list --unsorted

# Show public key
public:
    cargo run -- pub-key

# Find admin proposal block
admin-proposal-block:
    cargo run -- admin-proposal-block-height
